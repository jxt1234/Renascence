#include "transform.h"
TrBmp* TrHuffTransform(TrBmp* src, int precision)
{
    TrBmp* result = TrAllocBmp(precision, precision);
    int i, j;
    for (i=0; i<src->width; ++i)
    {
    }
    return result;
}
